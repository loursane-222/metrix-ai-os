import { describe, expect, it, vi } from "vitest";
import { resolveCustomFieldConversationPlan, validateCustomFieldPlannerOutput } from "../customer-custom-field-conversation-planner";
const custom = { fieldId: "customer.custom.definition-1", module: "customers", entityType: "customer", key: "bayi_bolgesi", label: "Bayi Bölgesi", description: "", valueType: "string", storageKind: "custom_value", requiredOnCreate: false, requiredOnUpdate: false, readable: true, writable: true, clearable: true, searchable: false, filterable: false, sortable: false, reportable: true, sourceOfTruth: "relation", sensitivity: "INTERNAL", riskLevel: "LOW", approvalPolicy: "NONE", permissionRead: "customers.read", permissionWrite: "customers.write", validation: {}, normalization: "trim", uiSection: "Özel Alanlar", uiOrder: 1, custom: true, state: "ACTIVE", aliases: [] as string[] } as const;
describe("AI-primary custom field planner", () => {
  it("resolves a model key to a trusted definition id", async () => { const generateText = vi.fn().mockResolvedValue(JSON.stringify({ intent: "UPDATE_CUSTOM_FIELD_DEFINITION", targetKey: "bayi_bolgesi", changes: { required: true } })); expect(await resolveCustomFieldConversationPlan({ utterance: "Bayi Bölgesi alanını zorunlu yap", fields: [custom], generateText })).toEqual({ intent: "UPDATE_CUSTOM_FIELD_DEFINITION", definitionId: "definition-1", input: { required: true } }); expect(generateText).toHaveBeenCalledOnce(); });
  it("rejects invented target keys and exact-key injection", () => { expect(validateCustomFieldPlannerOutput({ intent: "DEPRECATE_CUSTOM_FIELD", targetKey: "../../other-tenant" }, [custom])).toMatchObject({ intent: "CLARIFICATION_REQUIRED" }); expect(validateCustomFieldPlannerOutput({ intent: "DEPRECATE_CUSTOM_FIELD", targetKey: "bayi_bolgesi", actionName: "customer.delete" }, [custom])).toBeNull(); });
});
