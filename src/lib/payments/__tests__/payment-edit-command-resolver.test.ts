import { describe, expect, it, vi } from "vitest"; import { resolvePaymentEditCommand } from "../payment-edit-command-resolver";
describe("payment edit resolver", () => { it.each(["PAID", "CANCELLED"])("short-circuits %s", async (status) => { const generateText = vi.fn(async () => "{}"); await expect(resolvePaymentEditCommand({ utterance: "300 TL tahsilat kaydet", activeTab: "actions", context: { paymentTitle: "T", status, remainingAmount: 300, currency: "TRY" }, generateText })).resolves.toEqual({ kind: "resolved", resolution: { kind: "unsupported" } }); expect(generateText).not.toHaveBeenCalled(); }); it("rejects an amount above the real remainder", async () => { await expect(resolvePaymentEditCommand({ utterance: "500 TL tahsilat kaydet", activeTab: "actions", context: { paymentTitle: "T", status: "PARTIAL", remainingAmount: 300, currency: "TRY" }, generateText: async () => '{"result":"executable","action":"apply","amount":500}' })).resolves.toMatchObject({ kind: "resolved", resolution: { kind: "clarification_required" } }); }); });
