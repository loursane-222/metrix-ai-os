import { describe, expect, it, vi } from "vitest"; import { applySupplierEditCommand, type SupplierEditSurfaceRuntimeAdapter } from "../supplier-edit-command-apply"; import type { SupplierEditFieldName } from "../supplier-edit-command-contract";
const supplier = { id: "s", displayName: "A", legalName: null, phone: "1", email: null, website: null, taxNumber: null, taxOffice: null, metrixNote: null, riskNotes: null, status: "ACTIVE" as const, updatedAt: "" };
function runtime(): SupplierEditSurfaceRuntimeAdapter { const state: ReturnType<SupplierEditSurfaceRuntimeAdapter["getState"]> = { activeTab: "actions", draft: { displayName: "A", legalName: "", phone: "2", email: "", website: "", taxNumber: "", taxOffice: "", metrixNote: "", riskNotes: "", status: "ACTIVE" }, supplier }; return { getState: () => state, setField: vi.fn((field: SupplierEditFieldName, value: string) => { state.draft[field] = value; }), commit: vi.fn(async () => ({ ok: true })), discard: vi.fn() }; }
describe("apply supplier edit command", () => { it("sets, clears and reverts fields", async () => { const r = runtime(); await applySupplierEditCommand({ type: "set_field", field: "phone", value: "3" }, r); expect(r.getState().draft.phone).toBe("3"); await applySupplierEditCommand({ type: "clear_field", field: "phone" }, r); expect(r.getState().draft.phone).toBe(""); await applySupplierEditCommand({ type: "revert_field", field: "phone" }, r); expect(r.getState().draft.phone).toBe("1"); }); it("commits and discards through the surface", async () => { const r = runtime(); expect(await applySupplierEditCommand({ type: "commit" }, r)).toMatchObject({ status: "EXECUTED", commitOutcome: "SAVED" }); await applySupplierEditCommand({ type: "discard" }, r); expect(r.discard).toHaveBeenCalled(); }); });
