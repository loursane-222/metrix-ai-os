import type { ModuleFieldDefinition, ModuleFieldValueType } from "@/lib/field-authority/field-authority";
import { createDomainFieldRegistry } from "@/lib/edit-command/domain-field-registry";
const specs: Array<[string, string, ModuleFieldValueType, boolean, string[]?]> = [["displayName", "Tedarikçi adı", "string", false], ["legalName", "Ticari unvan", "string", true], ["phone", "Telefon", "phone", true], ["email", "E-posta", "email", true], ["website", "Web sitesi", "string", true], ["taxNumber", "Vergi no", "string", true], ["taxOffice", "Vergi dairesi", "string", true], ["metrixNote", "Not", "multiline_string", true], ["riskNotes", "Risk notları", "multiline_string", true], ["status", "Durum", "enum", false, ["ACTIVE", "PASSIVE", "ARCHIVED"]]];
export const SUPPLIER_EDIT_FIELDS: readonly ModuleFieldDefinition[] = specs.map(([key, label, valueType, clearable, options], index) => ({ fieldId: `supplier.${key}`, module: "suppliers", entityType: "Supplier", key, label, description: `${label} alanı`, valueType, storageKind: "scalar", requiredOnCreate: key === "displayName", requiredOnUpdate: false, readable: true, writable: true, clearable, searchable: false, filterable: key === "status", sortable: false, reportable: true, sourceOfTruth: "entity", sensitivity: key === "metrixNote" || key === "riskNotes" ? "SENSITIVE" : "PUBLIC", riskLevel: "LOW", approvalPolicy: "NONE", permissionRead: "suppliers.read", permissionWrite: "suppliers.write", validation: options ? { options } : {}, normalization: valueType === "email" ? "email" : "trim", uiSection: "Tedarikçi", uiOrder: index, custom: false, state: "ACTIVE", aliases: [label.toLocaleLowerCase("tr-TR")] }));
export const SUPPLIER_EDIT_FIELD_REGISTRY = createDomainFieldRegistry({ domain: "suppliers", entityType: "Supplier", fields: SUPPLIER_EDIT_FIELDS });
