import { createDomainFieldRegistry } from "@/lib/edit-command/domain-field-registry";
export const COMPANY_GOAL_CREATE_FIELD_REGISTRY = createDomainFieldRegistry({ domain: "company", entityType: "CompanyGoal", fields: [] });
