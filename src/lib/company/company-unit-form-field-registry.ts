import { createDomainFieldRegistry } from "@/lib/edit-command/domain-field-registry";
export const COMPANY_UNIT_FORM_FIELD_REGISTRY = createDomainFieldRegistry({ domain: "company", entityType: "CompanyUnit", fields: [] });
