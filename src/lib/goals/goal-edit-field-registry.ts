import type { ModuleFieldDefinition, ModuleFieldValueType } from "@/lib/field-authority/field-authority";
import { createDomainFieldRegistry } from "@/lib/edit-command/domain-field-registry";
const specs: Array<[string, string, ModuleFieldValueType, boolean, string[]?]> = [["title", "Başlık", "string", false], ["period", "Dönem", "enum", false, ["MONTHLY", "QUARTERLY", "YEARLY", "CUSTOM"]], ["revenue", "Hedef gelir", "money", true], ["collection", "Hedef tahsilat", "money", true], ["startsAt", "Başlangıç tarihi", "date", true], ["endsAt", "Bitiş tarihi", "date", true], ["status", "Durum", "enum", false, ["ACTIVE", "COMPLETED", "CANCELLED"]]];
export const GOAL_EDIT_FIELDS: readonly ModuleFieldDefinition[] = specs.map(([key, label, valueType, clearable, options], index) => ({ fieldId: `goal.${key}`, module: "goals", entityType: "SalesGoal", key, label, description: `${label} alanı`, valueType, storageKind: "scalar", requiredOnCreate: key === "title" || key === "period", requiredOnUpdate: false, readable: true, writable: true, clearable, searchable: false, filterable: key === "status", sortable: false, reportable: true, sourceOfTruth: "entity", sensitivity: "PUBLIC", riskLevel: "LOW", approvalPolicy: "NONE", permissionRead: "goals.read", permissionWrite: "goals.write", validation: options ? { options } : {}, normalization: "trim", uiSection: "Hedef", uiOrder: index, custom: false, state: "ACTIVE", aliases: [label.toLocaleLowerCase("tr-TR")] }));
export const GOAL_EDIT_FIELD_REGISTRY = createDomainFieldRegistry({ domain: "goals", entityType: "SalesGoal", fields: GOAL_EDIT_FIELDS });
