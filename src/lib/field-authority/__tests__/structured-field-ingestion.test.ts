import { describe, expect, it } from "vitest";
import { previewCandidatesInDraft, validateStructuredFieldCandidates } from "../structured-field-ingestion";
import { CUSTOMER_BUILT_IN_FIELDS } from "@/lib/customers/customer-field-registry";
describe("structured customer field ingestion boundary", () => { it("retains source/confidence, requires confirmation and only previews", () => { const candidates = validateStructuredFieldCandidates([{ fieldId: "customer.legalName", extractedValue: "Arda Yapı AŞ", confidence: 0.97, source: { sourceId: "doc-1", mediaType: "application/pdf", page: 1 }, warnings: [], conflicts: [] }, { fieldId: "customer.taxNumber", extractedValue: "1234567890", confidence: 0.91, source: { sourceId: "doc-1", mediaType: "application/pdf", page: 1 }, warnings: [], conflicts: [] }], CUSTOMER_BUILT_IN_FIELDS); expect(candidates.every((candidate) => candidate.requiresUserConfirmation)).toBe(true); expect(previewCandidatesInDraft(candidates, { "customer.legalName": "Eski" })).toMatchObject({ committed: false, conflicts: ["customer.legalName"], values: { "customer.taxNumber": "1234567890" } }); }); it("rejects unsupported and read-only candidates", () => { expect(() => validateStructuredFieldCandidates([{ fieldId: "customer.actorId", extractedValue: "x", confidence: 1, source: { sourceId: "x", mediaType: "x" } }], CUSTOMER_BUILT_IN_FIELDS)).toThrow("Unsupported"); expect(() => validateStructuredFieldCandidates([{ fieldId: "customer.balanceCents", extractedValue: 1, confidence: 1, source: { sourceId: "x", mediaType: "x" } }], CUSTOMER_BUILT_IN_FIELDS)).toThrow("Unsupported"); }); });
