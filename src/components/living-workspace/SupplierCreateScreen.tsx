"use client";
import { useState } from "react";
export function SupplierCreateScreen(){const[name,setName]=useState("");const[saved,setSaved]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/suppliers",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({displayName:name})});setSaved(r.ok)}return <form onSubmit={submit} className="mx-auto max-w-2xl space-y-4 p-6"><h1 className="text-2xl font-semibold">Yeni Tedarikçi</h1><label className="block text-sm">Tedarikçi adı<input required value={name} onChange={e=>setName(e.target.value)} className="mt-1 w-full rounded border p-2"/></label><button className="rounded bg-primary px-4 py-2 text-white" type="submit">Kaydet</button>{saved&&<p role="status">Tedarikçi kaydedildi.</p>}</form>}
