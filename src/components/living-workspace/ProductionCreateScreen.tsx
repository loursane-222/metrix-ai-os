"use client";
import { useState } from "react";
export function ProductionCreateScreen(){const[orderNumber,setOrderNumber]=useState("");const[quantityPlanned,setQuantityPlanned]=useState("");const[saved,setSaved]=useState(false);const[error,setError]=useState<string|null>(null);async function submit(e:React.FormEvent){e.preventDefault();setError(null);const r=await fetch("/api/production",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({orderNumber,quantityPlanned:Number(quantityPlanned)})});if(r.ok){setSaved(true);}else{const j=await r.json().catch(()=>null) as {error?:{message?:string}}|null;setError(j?.error?.message??"Üretim emri oluşturulamadı.");}}return <form onSubmit={submit} className="mx-auto max-w-2xl space-y-4 p-6"><h1 className="text-2xl font-semibold">Yeni Üretim Emri</h1><label className="block text-sm">Emir numarası<input required value={orderNumber} onChange={e=>setOrderNumber(e.target.value)} className="mt-1 w-full rounded border p-2"/></label><label className="block text-sm">Planlanan miktar<input required type="number" min="0" step="0.001" value={quantityPlanned} onChange={e=>setQuantityPlanned(e.target.value)} className="mt-1 w-full rounded border p-2"/></label><button className="rounded bg-primary px-4 py-2 text-white" type="submit">Kaydet</button>{saved&&<p role="status">Üretim emri oluşturuldu.</p>}{error&&<p role="alert">{error}</p>}</form>}
