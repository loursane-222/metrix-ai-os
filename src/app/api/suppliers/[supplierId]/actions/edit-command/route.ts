import { ok } from "@/lib/api/response"; import { readJsonObject, requiredString } from "@/lib/api/validation"; import { requireAuthContextFromCookies } from "@/lib/auth/guards/api-auth-guard"; import { mapExecutionErrorToHttpResponse } from "@/lib/action-runtime/gateway/execution-http-errors"; import { resolveSupplierEditCommand } from "@/lib/suppliers/supplier-edit-command-resolver"; import { generateSupplierEditCommandText } from "@/lib/suppliers/supplier-edit-command-ai-adapter";
export const maxDuration = 60; export async function POST(request: Request, context: { params: Promise<{ supplierId: string }> }): Promise<Response> { try { await requireAuthContextFromCookies(); await context.params; const body = await readJsonObject(request); const outcome = await resolveSupplierEditCommand({ utterance: requiredString(body, "utterance"), activeTab: requiredString(body, "activeTab"), generateText: generateSupplierEditCommandText }); return ok({ outcome }); } catch (error) { return mapExecutionErrorToHttpResponse(error); } }
