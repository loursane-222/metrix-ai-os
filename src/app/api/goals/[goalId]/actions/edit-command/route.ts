import { ok } from "@/lib/api/response"; import { readJsonObject, requiredString } from "@/lib/api/validation"; import { requireAuthContextFromCookies } from "@/lib/auth/guards/api-auth-guard"; import { mapExecutionErrorToHttpResponse } from "@/lib/action-runtime/gateway/execution-http-errors"; import { resolveGoalEditCommand } from "@/lib/goals/goal-edit-command-resolver"; import { generateGoalEditCommandText } from "@/lib/goals/goal-edit-command-ai-adapter";
export const maxDuration = 60; export async function POST(request: Request, context: { params: Promise<{ goalId: string }> }): Promise<Response> { try { await requireAuthContextFromCookies(); await context.params; const body = await readJsonObject(request); const outcome = await resolveGoalEditCommand({ utterance: requiredString(body, "utterance"), activeTab: requiredString(body, "activeTab"), generateText: generateGoalEditCommandText }); return ok({ outcome }); } catch (error) { return mapExecutionErrorToHttpResponse(error); } }
