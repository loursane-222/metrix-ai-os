import { ok } from "@/lib/api/response"; import { readJsonObject, requiredString } from "@/lib/api/validation"; import { requireAuthContextFromCookies } from "@/lib/auth/guards/api-auth-guard"; import { mapExecutionErrorToHttpResponse } from "@/lib/action-runtime/gateway/execution-http-errors"; import { resolveProductEditCommand } from "@/lib/products/product-edit-command-resolver"; import { generateProductEditCommandText } from "@/lib/products/product-edit-command-ai-adapter";
export const maxDuration = 60; export async function POST(request: Request, context: { params: Promise<{ productId: string }> }): Promise<Response> { try { await requireAuthContextFromCookies(); await context.params; const body = await readJsonObject(request); const outcome = await resolveProductEditCommand({ utterance: requiredString(body, "utterance"), activeTab: requiredString(body, "activeTab"), generateText: generateProductEditCommandText }); return ok({ outcome }); } catch (error) { return mapExecutionErrorToHttpResponse(error); } }
