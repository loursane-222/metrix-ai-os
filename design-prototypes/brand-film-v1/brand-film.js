const stage=document.querySelector('.stage');const q=new URLSearchParams(location.search);const allowed=['ready','playing','paused','error'];stage.dataset.state=allowed.includes(q.get('state'))?q.get('state'):'ready';const primary=document.querySelector('.primary'),secondary=document.querySelector('.secondary'),playCue=document.querySelector('.play-cue');function start(){stage.dataset.state='playing'}function toggle(){stage.dataset.state=stage.dataset.state==='playing'?'paused':'playing'}primary.addEventListener('click',()=>stage.dataset.state==='ready'||stage.dataset.state==='error'?start():toggle());playCue.addEventListener('click',start);secondary.addEventListener('click',()=>stage.dataset.state='ready');
