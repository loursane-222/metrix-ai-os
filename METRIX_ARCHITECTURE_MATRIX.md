# METRIX — Living Constitution Audit

Snapshot date: 2026-08-02. HEAD at time of audit: `40e4ad4`.

Method: the full Foundation ZIP (`/Users/mac/Desktop/METRIX FOUNDATION/Metrix_Foundation.zip`, 29 `Domain_Sözleşme` constitutions + 7 Executive Core/Runtime documents) cross-referenced against `prisma/schema.prisma`, `src/lib/action-runtime/registry/manifests/*`, `src/lib/action-runtime/domains/*`, `src/lib/conversation-extensions/*`, `src/lib/living-workspace/*`, and direct authenticated production testing at `https://metrixgm.com`. Status vocabulary is the manifesto's own: **YAŞAMIYOR / KISMEN YAŞIYOR / PRODUCTION'DA YAŞIYOR / ACCEPTED**. This document is the operation's living backlog — update it at the end of every living-behavior cycle, do not let it go stale (it was ~14 commits stale before this rewrite).

This document supersedes its own 2026-08-01 version (Uygulanmış/Kısmen/Hiç-uygulanmamış vocabulary, 20-domain scope) and the 2026-07-25 `METRIX_Constitution_Audit.md` (Executive Core reasoning-layer audit, folded into §1 below).

---

## 1. Executive Core / reasoning-layer verdict

Re-verified 2026-08-02 against the 2026-07-25 static audit's three P0 findings. **All three are now fixed:**

1. **Gap-intercept bypass** (LLM-free canned answers) — FIXED. `detectExecutiveGap`'s output is now folded into `managerAdviceAugmentationContext.executiveGapSignal`, a prompt-context signal, not a returned answer. Regression-tested (`single-authority-source-contract.test.ts`).
2. **Silent mock-provider fallback** — FIXED. `resolveConfiguredAiProvider` (`src/lib/ai/providers/provider-policy.ts`) now throws `AiProviderConfigurationError` (→ HTTP 503) on any misconfiguration outside dev/test; no silent substitution.
3. **Unapproved automatic status changes** — FIXED. `completeExecutiveAction`/`applyCollectionActionLifecycle`/`applyQuoteWorkflowLifecycle` are no longer called from the chat route. Lifecycle mutation now goes exclusively through `action-runtime` (`collection.set_lifecycle`, `quote.set_lifecycle`, `executive_action.complete`), all `riskLevelBase: "HIGH"` + `approvalPolicy: "EXPLICIT"`. The old keyword-confidence detectors still exist as files but have zero production callers — orphaned, not dangerous.

`buildUniversalHandoffMessage` (commit `fbeb6af`) now sits downstream of both bypass points and deterministically overrides `aiContent` for any domain in `CONVERSATION_EXTENSION_DOMAINS` — single authority confirmed structurally, not just by convention.

**Verdict: PRODUCTION'DA YAŞIYOR**, no open P0s. Not re-audited line-by-line this session beyond the three specific findings; a fresh full pass would still be prudent every few months as new domains attach to this pipeline.

## 2. Living Workspace architecture verdict

`WORKSPACE_DOMAINS` (`src/lib/living-workspace/contracts.ts`) now covers **customer, company, product, task, offer, notification** — 6 domains, each with a real directive/surface adapter and inline `presentation="living"` rendering on the single `/metrix` route. URL stays fixed, chat panel stays mounted, confirmed for customer/offer/task by direct production testing this operation and the prior one.

Remaining routed pages still rendering the literal `<UnavailableBusinessSurface />` stub: `accounting, collections, company-dna, daily-rhythm, documents, finance, goals, opinion, reports, sales, suppliers, team, templates, work-plan`.

## 3. Domain-by-domain classification (all 29 Foundation `Domain_Sözleşme` constitutions)

| # | Domain (TR/EN) | Mandated behavior (terse) | Status | Evidence |
|---|---|---|---|---|
| 01 | İşletme / Business (meta) | Single-truth-owner per fact; Executive Intelligence reads, never owns data | PRODUCTION'DA YAŞIYOR | Structural doctrine, reflected by Action Runtime + canonical Prisma models pattern across all ACCEPTED domains |
| 02 | Şirket / Company | Create/update company identity, branches, assets via conversation; feed Exec Dashboard | PRODUCTION'DA YAŞIYOR | `company.actions.ts`, `CompanyProfile/Unit/Asset/DataSource`, Living Workspace domain — **not re-verified with authenticated production acceptance this operation** (user deferred; see `METRIX_OPERATION_HANDOFF.md` §7) |
| 03 | Müşteri / Customer | Create/enrich customer via conversation; feed Offer/Collection context | **ACCEPTED** | Production-verified prior operation |
| 04 | Ürün / Product | Represent any sellable value as one Product | PRODUCTION'DA YAŞIYOR | `products.actions.ts`, `ProductService`, Living Workspace domain (read/list/detail); create/edit surface depth not fully confirmed — deferred, same as Company |
| 05 | Stok / Stock | Track physical qty/location/lot separate from Product identity | YAŞAMIYOR | No model beyond a `stock` scalar field on `ProductService`; no manifest, no workspace domain |
| 06 | Finansal / Finance | Canonical revenue/cost/margin/cash model; margin check before offers | YAŞAMIYOR | No model, no manifest, no workspace domain |
| 07 | Üretim / Production | Canonical capacity/work-center/BOM/route model | YAŞAMIYOR | Nothing in schema, manifests, or workspace |
| 08 | Tedarikçi / Supplier | Canonical supplier identity, performance/price history | YAŞAMIYOR | No model/manifest/workspace; `/metrix/suppliers` is a stub |
| 09 | Teklif / Offer (Quote) | Create/revise/send offers via conversation | **ACCEPTED** | Production-verified (`0cde6ad`+`99f4af5`), Living Workspace integrated |
| 10 | Sipariş / Order | Convert accepted offer to operational commitment | YAŞAMIYOR | No model, manifest, or workspace domain |
| 11 | İrsaliye / Delivery Note | Physical dispatch tracking distinct from Order | YAŞAMIYOR | No model, manifest, or workspace domain |
| 12 | Fatura / Invoice | Canonical invoice tied to order/delivery, tax, e-Fatura | **ACCEPTED** (DRAFT create + list only) | This operation, commit `ae6efb0` — see §7. Multi-line items, SENT/PAID transitions, and real e-Fatura submission remain YAŞAMIYOR, deliberately out of this slice's scope. |
| 13 | Tahsilat / Collection & Payment | Record payments/allocations via conversation | **ACCEPTED** (create + list + mark-as-paid/partial + real OVERDUE + AI dunning review, full chain production-verified) | Commits `b7f3a2e` (create+list), `0dc83ce` (payment.apply mark-as-paid), `bae5d64` (CollectionAction AI dunning review — wired but initially unprovable with real data), `40e4ad4` (Payment Lifecycle fix: real OVERDUE reconciliation + due-date parsing + genuine partial-payment UI — see §11). Full loop re-verified with organically-created production data: real conversational payment with a due-date clause → real OVERDUE → real AI suggestion → real partial payment via UI → real Tamamlandı via Action Runtime, SUCCESS. `collection.start` remains unwired (its schema — `paymentId`+`customerId`, no `collectionActionId` — cannot target an existing suggested action; left untouched, no invented semantics). Multi-channel dunning communication (WhatsApp/e-posta hazırlama) and cari hesap ekstresi/mutabakat remain YAŞAMIYOR, out of scope. |
| 14 | Belge / Document | Canonical Document identity across versions/sources; OCR | YAŞAMIYOR (generic) | Only `CustomerDocumentAttachment` (customer-scoped); no generic Document model, no OCR |
| 15 | Muhasebe / Accounting | Canonical journal/ledger/fiscal periods | YAŞAMIYOR | Only unrelated `Expense` model; no Turkish tax/chart-of-accounts logic anywhere |
| 16 | Ekip / People (Team) | Canonical person identity separate from role; org structure | KISMEN YAŞIYOR | Generic `Person` model exists; no manifest, no workspace surface |
| 17 | Görev / Task | Create/assign tasks via conversation, Living Workspace | **ACCEPTED** | This operation — see `METRIX_OPERATION_HANDOFF.md` §2 |
| 18 | Takvim / Calendar | Canonical time model for meetings/scheduling | YAŞAMIYOR | `Event` model is an audit log, not scheduling; no manifest/workspace |
| 19 | Bildirim / Notification | Route events to right person/channel; read tracking | **ACCEPTED** | This operation — see `METRIX_OPERATION_HANDOFF.md` §2 |
| 20 | Hedef / Goals & KPI | Canonical goal/KPI, target vs actual | KISMEN YAŞIYOR | `SalesGoal`/`KpiDefinition` models; only a `company.goal.upsert` action (no dedicated manifest); `/metrix/goals` is a stub |
| 21 | Raporlama / Reporting & Analytics | Canonical report/insight model, conversation-first analysis | KISMEN YAŞIYOR | Strong data layer (`ReportTemplate/Assignment/Submission/Answer/MetricSnapshot`); no manifest, no workspace domain, `/metrix/reports` is a stub — best backend-to-UI ratio of any unbuilt surface |
| 22 | Kurumsal Hafıza / Executive Memory & Knowledge | Verified-knowledge store distinct from raw chat | KISMEN YAŞIYOR | `Memory/MemoryItem/MemoryCandidate` + substantial engine in `src/lib/core/memory-candidates/`; no dedicated workspace surface (used internally, not user-facing) |
| 23 | Yönetici Karar Motoru / Executive Decision Engine | Canonical decision record, alternatives, outcome tracking | KISMEN YAŞIYOR | `ExecutiveDecisionRecord/Outcome`, `ExecutiveSignalSnapshot`; no manifest, no workspace domain |
| 24 | Yönetici Eylem Motoru / Executive Action Engine | Governed, approvable, reversible cross-domain execution | KISMEN YAŞIYOR → PRODUCTION'DA YAŞIYOR (infra) | `ExecutiveAction` model + `executive-actions.actions.ts` manifest + dedicated handler dir; functions as infrastructure other domains use, not a standalone user surface |
| 25 | Yönetici İletişim Motoru / Executive Communication Engine | Canonical outbound-communication model | YAŞAMIYOR | No model, manifest, or workspace; ad hoc messaging exists inside conversation-extensions only |
| 26 | Yönetici Orkestrasyon Motoru / Executive Orchestration Engine | Cross-domain execution graph/sequencing | KISMEN YAŞIYOR | `ExecutiveRuntimeTraceRecord` model; Action Runtime gateway layer functionally orchestrates without an explicit canonical Orchestration domain |
| 27 | Konuşma ve Diyalog / Conversation & Dialogue | Single canonical conversation/session/turn model | **PRODUCTION'DA YAŞIYOR** | `Conversation/Message` models, `src/lib/conversation-extensions/*` — the mechanism behind the single-authority fix (§1); this is the domain every other domain's conversational entry point is built on |
| 28 | Entegrasyon / Integration | Canonical connection/auth/sync across external systems | KISMEN YAŞIYOR | Only Gmail implemented (`GmailConnection` + `src/lib/integrations/gmail/`); no generalized multi-provider domain |
| 29 | Platform Çalışma / Platform Operating Constitution | Identity/auth/authorization/audit substrate | **PRODUCTION'DA YAŞIYOR** (infra) | `User/Organization/OrganizationMember/Session/OtpChallenge/TrustedDevice/ActionApproval` — real, load-bearing, underlies every ACCEPTED domain above |

## 4. Next living behavior — recommendation

Domain #13 (Tahsilat) is now fully closed (create + list + mark-as-paid + AI dunning review, see §10), and domain #12 (Fatura) create-slice is closed — see §6/§7/§9/§10. For the next operation, ranked:

1. **Complete Fatura's SENT transition + link an Invoice to its originating Quote automatically** — closes the Offer→Invoice→Payment loop the last operations built piece by piece. No new Prisma models needed. Smallest possible next increment now that Tahsilat's slice is done.
2. **Muhasebe (#15, Accounting)** — now unblocked. Its own constitution defines its value as auto-posting Invoice/Payment events; both now exist (create-level, and Payment now has a real PAID transition). A minimal auto-post-on-invoice-create/auto-post-on-payment-apply journal entry is a genuinely new, real capability, not a standalone disconnected ledger tool anymore.
3. **Raporlama (#21)** — strongest backend-to-UI ratio of any domain with zero UI (`ReportTemplate/Assignment/Submission/Answer/MetricSnapshot` all real).
4. **Hedef (#20, Goals/KPI)** — smaller lift than Raporlama, lower day-to-day value.
5. **Executive Onboarding / Executive Subscription & Licensing (new domains #30/#31, see §8)** — much larger, business-model-defining lifts; not ranked alongside the above single-verb slices. Treat as a separate future operation, not a "next increment."

## 5. Explicitly NOT touched, and why (carried forward, still true)

- **`/metrix/accounting` vs `/metrix/finance`**: two distinct nav concepts in the legacy design, not a duplicate. Needs a product answer (cash/treasury vs. bookkeeping scope), not a code answer. Left alone.
- **`executive-*-intelligence`/`executive-*-signal`/`executive-decision-*` clusters**: real cross-imports exist between them (layered, not parallel) — collapsing needs design review, not a blind merge.

## 6. Tahsilat (Collection & Payment) — closed this operation

Commit `b7f3a2e`, pushed, deployed, production-verified (real account, real "Atlas Insaat" customer). Scope: recording a payment via conversation only (`"<customer> için <amount> TL tahsilat kaydet"` → resolves customer via the existing `resolveCustomerReference` utility, creates a real `Payment` row through the existing, already-live `POST /api/payments` endpoint — no new execution authority), plus a canonical `/metrix/collections` list surface replacing the stub.

**Real bug fixed while wiring this in**: `LivingWorkspaceHost.tsx`'s generic entity-list data loader only recognized `customers`/`products`/`tasks` as response keys — any domain relying on the generic list surface (this one included) would always render "Kayıt bulunamadı" even with real data. Fixed by adding `payments` to the recognized-key fallback chain. This was a latent gap in shared infrastructure, not something the Payment domain introduced — worth checking if `notifications`/`quotes` are fully covered too the next time either domain's generic list path is touched (notifications has its own dedicated branch already; quotes' bare list route has never been exercised through this generic path, per `offer-management-conversation-extension.ts` always navigating to a specific `quoteId`).

Production acceptance evidence: "Atlas Insaat için 7500 TL tahsilat kaydet" → real `Payment` row (`id cmsbqzjfo000004ju0oxbazsk`), inline Living Workspace open (URL stayed `metrixgm.com`, chat panel stayed mounted), `GET /api/payments` confirms server persistence, fresh navigation to `/metrix/collections` (not conversation cache) shows the same record. Negative path ("Bilinmeyen Firma için...") verified in local dev only (same code, not environment-dependent) — correctly returns the deterministic NOT_FOUND clarification message, no fabrication.

**Deliberately not built** (documented, not a defect): marking a payment as PAID, the `CollectionAction` AI-suggested dunning/reminder workflow, reopening an existing payment by name via conversation. All three have real, untouched infrastructure ready for a future operation (see §4).

## 7. Fatura (Invoice) — closed this operation

Commit `ae6efb0`, pushed, deployed, production-verified (real account, real "Atlas Insaat" customer). Unlike Tahsilat, Invoice had zero pre-existing infrastructure — built as a brand-new domain end-to-end, following the full modern Action Runtime pattern (Prisma model + migration, domain service, Action Registry manifest, handler, gateway, composition-root registration) exactly mirroring Task's reference chain, not the legacy shortcut Payment reused. Scope: creating an internal DRAFT invoice via conversation only (`"<customer> için <amount> TL fatura kes"`), single consolidated amount + 20% KDV tax computed and stored at creation, auto-numbered `FTR-{year}-{seq}`, plus a canonical `/metrix/invoices` list surface (new route, nothing replaced).

**Real bug fixed while wiring this in**: the role→permission map in `execution-context.ts` had no `"invoices.write"` entry for any role — `invoice.create` failed policy evaluation with a 403 for every actor regardless of role (OWNER included), before ever reaching the handler. Added `invoices.write` to OWNER/EXECUTIVE/MANAGER, matching where `payments.write`/`collections.write` already sit (not TEAM_LEAD/EMPLOYEE).

**Methodology note, not a product defect**: initial production acceptance testing appeared to fail (utterance fell through to free-form AI reasoning, no extension match) — root-caused to the browser tab having loaded its JS bundle from before the deploy fully propagated; a cache-busted reload confirmed the new code and the identical utterance then worked correctly. Recorded here so a future session doesn't mistake this for a real regression if it looks similar — always confirm the actually-loaded bundle contains the new code (e.g. via `performance.getEntriesByType('resource')` + fetch-and-grep) before treating an unexpected AI response as evidence of a bug.

Production acceptance evidence: "Atlas Insaat için 20000 TL fatura kes" → real `Invoice` row (`id cmsbwkcnu000004jreekfg26s`, `FTR-2026-0001`, amount 20000, taxAmount 4000, totalAmount 24000 — tax math verified correct), inline Living Workspace open (URL stayed `metrixgm.com`, chat panel stayed mounted), `GET /api/invoices` confirms server persistence, fresh navigation to `/metrix/invoices` (not conversation cache) shows the same record with the same totals.

**Deliberately not built** (documented, not a defect): multi-line item breakdown, SENT/PAID/CANCELLED status transitions, real e-Fatura government submission, linking an invoice to its originating Quote automatically, reopening an existing invoice by name via conversation. All are real, well-scoped follow-ons for a future operation (see §4).

## 8. Foundation sync — `Metrix_Foundation_2.zip` (2026-08-02, same session)

`Metrix_Foundation_2.zip` (Desktop, 17:33) supersedes the `Metrix_Foundation.zip` (13:48) this audit was originally built against. Diffed by exact zip member list — three files are new relative to the original: `METRIX Executive Onboarding Anayasası 1.0.docx`, `METRIX Executive Subscription & Licensing Anayasası 1.0.docx`, and `METRIX Living Product Completion Operasyon Manifestosu 1.0.docx ⭐.docx` (the last one is the manifesto already driving this document's methodology — not a new domain). `Presence, Awareness ve Personalization Anayasası` was already inside the original zip, not new.

Both new domains were read in full and checked against the codebase (not built — logged for future ranking, see §4 item 6):

- **#30 Executive Onboarding** — first-run experience, Company Discovery via conversation, Excel-as-first-class-data-source, optional "Executive Orientation" intro chat, auto-starting 14-day Trial, seamless hand-off into Living Workspace. **KISMEN YAŞIYOR**: substantial pre-existing infra (`OnboardingStatus` enum + fields on org/user, `/api/onboarding/{start,discovery,save,complete,status}` routes, voice onboarding UI, an `onboarding-v3` page) — but none of it has been touched by this operation series (last real commit `ea28b26`, otherwise initial import), and it could not be confirmed reachable from the live entry flow (no reference from `page.tsx`/`metrix-onboarding-app.tsx`). Whether it's actually live in production is **unverified**, not confirmed-dead and not confirmed-working.
- **#31 Executive Subscription & Licensing** — company-centric (not user-centric) licensing, a single Executive Intelligence across all plan tiers, auto-starting Trial, transparent billing, no data loss on plan change, no manipulative upsell pressure. **YAŞAMIYOR**: zero infrastructure. No `Plan`/`Package`/`Tier`/`Billing`/`Subscription`/`License` model anywhere in `prisma/schema.prisma`. Would be a brand-new, business-model-defining domain built from scratch — materially larger than any single-conversation-verb slice built so far (Task/Payment/Invoice).

## 9. Fourth operation (same session) — Tahsilat mark-as-paid (`payment.apply`): ACCEPTED

Commit `0dc83ce`, pushed, deployed, production-verified (real account, real "Atlas Insaat" `Payment` `cmsbqzjfo000004ju0oxbazsk`). Continuation of §6's Tahsilat slice: `payment.apply`'s Action Registry entry existed but its `inputSchema` (`customerId`/`quoteId`/`amount`, no `paymentId`) could never actually reference an existing Payment row — fixed to bind `paymentId` (the entity actually being mutated) + `amount`. Built full modern Action Runtime wiring (handler with the same critical/non-critical side-effect split as `invoiceCreateHandler`, composition-root registration) plus a request→confirm→execute approval gateway (`payment-apply-gateway.ts`) mirroring `customer-archive-gateway.ts`/`quote-dispatch-gateway.ts` exactly — `payment.apply` is `riskLevelBase: HIGH` + `approvalPolicy: CONDITIONAL`, which the policy engine enforces for real (`REQUIRES_APPROVAL`, not just descriptive metadata).

**Trigger surface decision**: user explicitly directed this be built as one canonical Action Runtime capability (gateway + client functions), not owned by a single page — `/metrix/collections` is only the *first* production entry point (a row-level "Ödendi olarak işaretle" button + inline Onayla/Vazgeç confirm, mirroring `CustomerEditScreen`'s archive confirm UX exactly). Any other Living Workspace surface can call the same `requestPaymentApplyAction`/`confirmPaymentApplyAction`/`cancelPaymentApplyAction` client functions (`src/lib/payments/payments-client.ts`) and hits the identical gateway/Action Runtime/persistence/notification chain — no second mutation authority was created. A conversational (free-text) trigger was deliberately not built this operation — there is no precedent anywhere in the codebase for a HIGH-risk/CONDITIONAL approval flow through conversational back-and-forth (only UI-button request/confirm exists as a proven pattern today); adding one would have been new architecture, not a reuse.

**Production acceptance evidence**: clicked "Ödendi olarak işaretle" on the real `Payment` row on `/metrix/collections` → `POST /api/payments/cmsbqzjfo000004ju0oxbazsk/actions/apply` (`operation: "request"`) → 200, inline Onayla/Vazgeç shown → clicked Onayla → same route (`operation: "confirm"`) → 200, row's `DURUM` flipped to `PAID` inline. Independent fresh page load (not client cache) of `/metrix/collections` confirms `DURUM: PAID` server-side. `/metrix/notifications` shows a real `Tahsilat tamamlandı` / `Atlas Insaat Tahsilatı — 7500 TRY` notification — non-critical side effect delivered.

**Methodology note**: initial post-deploy check found the loaded JS bundle didn't yet contain the new client code (confirmed via `performance.getEntriesByType('resource')` + fetch-and-grep for `requestPaymentApplyAction`, per the Fatura operation's documented method) — Vercel propagation lag, not a regression; a forced reload after the bundle updated resolved it. A tight curl polling loop against the production domain to detect propagation triggered Vercel's bot-protection checkpoint once (transient, cleared on the next real navigation) — future sessions should prefer a single wait + single re-check over a fast poll loop, consistent with the "no deploy polling" convention.

**Self Review**: Kurucu anayasa korundu (PASS — Tahsilat Anayasası'nın kapsamındaki bir davranış, yeni anayasa icat edilmedi). Living Workspace korundu (PASS — URL sabit kaldı, inline açılış). Single Authority korundu (PASS — tek gateway, tüm yüzeyler aynı yolu çağırır). İkinci authority oluştu mu? PASS (hayır, oluşmadı). Kullanıcı gerçekten yeni bir yaşayan davranış kazandı mı? PASS — tahsilat artık gerçekten ödendi olarak işaretlenebiliyor, production'da doğrulandı. **FAIL yok → ACCEPTED.**

**Deliberately not built** (documented, not a defect): free-text conversational trigger (see above), partial-payment UI (the service layer supports it — `applyPaymentAmount` computes PARTIAL vs PAID correctly — but the row button always applies the full remaining balance), negative-path browser verification (already-PAID / over-amount rejection is enforced in `payment.service.ts` and covered by the existing test suite, but not re-verified live in production this operation, matching how Tahsilat's original negative path was only verified in local dev).

## 10. Fifth operation (new session) — CollectionAction AI dunning/follow-up review: IMPLEMENTED_PENDING_ACCEPTANCE at the time, corrected to ACCEPTED by §11

**User correction (2026-08-02, same session)**: this section originally closed as ACCEPTED. The user reviewed it and correctly rejected that verdict: production acceptance requires the *actual* Payment lifecycle events (a real PARTIAL or OVERDUE payment) to occur in production, not just the review/action-runtime half of the loop. Since no code path could ever produce a real OVERDUE payment and no UI could produce a real PARTIAL one, the suggestion→review loop had never been exercised with real data — only with a temporary local-dev code edit (see below). Verdict downgraded to IMPLEMENTED_PENDING_ACCEPTANCE; §11 closes the gap and re-establishes ACCEPTED with genuine production evidence. Kept here verbatim for history — do not re-read this section for current status, see §11.

Commit `bae5d64`, pushed, deployed, production-verified (real account, real org, `GET /api/collection-actions` live at 200 on `metrixgm.com`). Per the handoff's own instruction, selected directly from §4 item 1 without rediscovery: the `CollectionAction` data model, `syncAiCollectionActions` suggestion generator, and `collection.set_lifecycle` Action Runtime handler all pre-existed but had zero product callers (`collection.set_lifecycle` was registered but never invoked; `syncAiCollectionActions` had zero non-test callers anywhere).

**Scope**: `GET /api/collection-actions` (new route) calls `syncAiCollectionActions` then lists active rows — the first real caller of either function. A new "Tahsilat Aksiyonları" panel (`CollectionActionsPanel.tsx`) renders on `/metrix/collections` above the existing Payment list, showing AI-suggested follow-ups (title, reason, remaining amount) with Tamamlandı/Reddet buttons. Both buttons drive `collection.set_lifecycle` (DONE/DISMISSED) through a new request→confirm→execute gateway (`collection-lifecycle-gateway.ts`) mirroring `customer-archive-gateway.ts` exactly (same HIGH-risk/EXPLICIT approval class) — no new authority. `collection.start` was deliberately left unwired: its manifest schema (`paymentId`+`customerId`, no `collectionActionId`) cannot reference an existing suggested action, so wiring it would have required inventing unspecified semantics; not needed since `collection.set_lifecycle`'s existing `IN_PROGRESS/DONE/DISMISSED` enum already covers the full review lifecycle.

**No production Payment currently reaches OVERDUE or PARTIAL through any live UI** (a pre-existing gap, not introduced or fixed here — no code path anywhere sets `Payment.status = OVERDUE`, and no partial-payment UI exists, only `payment.apply`'s full-remaining-balance button). This means the AI-suggestion panel is real and correct but currently has no naturally-occurring data to display in production. Full end-to-end verification (suggestion generation from a real PARTIAL payment, panel rendering, both Tamamlandı/DONE and Reddet/DISMISSED transitions through the real Action Runtime with `SUCCESS` across all 9 execution stages) was done in local dev by temporarily pointing the existing "mark as paid" button at a partial amount instead of the full remaining balance (reverted immediately after, zero diff vs. committed code) — same precedent as Tahsilat's and payment.apply's negative paths, which were also local-dev-only for the same class of reachability gap.

**Self Review**: Kurucu anayasa korundu (PASS — Tahsilat Anayasası'nın §8 "AI Genel Müdür tahsilat önceliklerini belirler" maddesinin kapsamındaki bir davranış). Living Workspace korundu (PASS — `/metrix/collections` URL sabit, panel `PaymentCanonicalScreen` içinde inline). Single Authority korundu (PASS — tek gateway, `collection.set_lifecycle`'ın registered handler'ı zaten vardı, ikinci bir authority oluşmadı). İkinci authority oluştu mu? PASS (hayır). Kullanıcı gerçekten yeni bir yaşayan davranış kazandı mı? PASS — AI'nın önerdiği tahsilat aksiyonları artık gerçekten görülüp kapatılabiliyor. **FAIL yok → ACCEPTED.**

**Deliberately not built** (documented, not a defect): `collection.start` (see above, no invented semantics), multi-channel dunning communication prep (WhatsApp/e-posta — separate Foundation-described capability, needs its own draft/send/approve loop), cari hesap ekstresi/mutabakat, a fix for the underlying OVERDUE-never-set / no-partial-payment-UI gap (out of this slice's scope — flagged here for whichever future operation touches Payment lifecycle next).

## 11. Sixth operation (same session) — Payment Lifecycle fix (real OVERDUE + genuine partial payment): ACCEPTED

Commit `40e4ad4`, pushed, deployed, production-verified with **organically-created production data** (real account, real "Atlas Insaat" customer) — closes the gap §10 was correctly called out on. Root cause: `Payment.status` could never reach `OVERDUE` (no code path anywhere wrote it) and could never reach `PARTIAL` through any real UI (only a full-remaining-balance "mark as paid" button existed) — so domain #13's AI dunning review (§10) had real infrastructure but no way to ever see real data in production.

**Scope**:
- `reconcileOverdueStatuses` (`payment.repository.ts` + `payment.service.ts`): a deterministic, rule-based status transition — `PENDING`/`PARTIAL` payments past `dueDate` become `OVERDUE`. This is exactly the Tahsilatlar Anayasası's own "Otomatik Alanlar: Durum" contract (an automatically-computed field), not a new mutation authority — no Action Registry entry, no approval, same class of mechanism as the existing CollectionAction sync. Called from `listPayments()` (every Payment read reflects true status) and from `syncAiCollectionActions` (AI suggestions see fresh OVERDUE data).
- `payment-management-conversation-extension.ts`: the conversational payment-create utterance now optionally parses a real due-date clause — `"vadesi N gün önce geçti"` (already-overdue, a real business recording an existing debt) or `"N gün vadeli"` (future due) — the *only* way a due date, and therefore OVERDUE, becomes reachable for a real Payment. Deterministic regex, same style as the existing pattern, backward compatible (omitting the clause behaves exactly as before).
- `LivingWorkspaceHost.tsx` `PaymentRow`: the always-full-balance button became a genuine editable amount input (defaults to remaining, clamped to `(0, remaining]`), still calling the exact same `payment.apply` gateway/Action Runtime path — no new authority, no second mutation surface.

**Production acceptance evidence** (real account, real "Atlas Insaat" customer, no fabricated records): conversational utterance *"Atlas Insaat için 2200 TL tahsilat kaydet, vadesi 15 gün önce geçti"* → real `Payment` created with `dueDate` 15 days in the past → `GET /api/payments` shows `status: "OVERDUE"` (computed, not hardcoded) → `GET /api/collection-actions` shows a real AI suggestion (`REMINDER`, `"15 gün gecikme, ₺2.200 bekliyor."`) → `/metrix/collections` panel renders it → entered `700` in the new amount input, clicked "Tahsil edildi olarak işaretle" → `Onayla (₺700)` → `POST /api/payments/{id}/actions/apply` → real `paidAmount: 700`, `status: OVERDUE` (still overdue, correctly — a partially-paid-but-still-late receivable) → clicked "Tamamlandı" on the AI suggestion → request→confirm through `collection.set_lifecycle` → `SUCCESS`, all 9 Action Runtime stages completed → independent re-fetch of `/api/collection-actions` shows the resolved suggestion replaced by a fresh one for the still-outstanding ₺1.500 balance (self-consistent AI behavior, not a bug — the underlying receivable is still overdue).

**Methodology note**: the first production test of this operation (before this commit was pushed) appeared to fail identically to the Fatura operation's documented false alarm — but this time the cause was different and worth recording precisely: it wasn't a stale bundle, it was *no bundle at all* — the code had only been verified in local dev and was never committed/pushed before testing against `metrixgm.com`. A bundle string search for `"vadesi"` gave a false positive (matched an unrelated pre-existing Customer field-label dictionary), which briefly suggested a real regex bug; only reproducing the exact dispatcher chain in a unit test (proving the code correct) and then checking `git status` (proving it was uncommitted) resolved it. Lesson: verify code is actually deployed (commit + push + propagation) before diagnosing "why didn't my change work in production" as a code bug.

**Self Review**: Kurucu anayasa korundu (PASS — Tahsilatlar Anayasası'nın kendi "Otomatik Alanlar" ve "Kısmi tahsilat... doğal olarak desteklenir" maddeleri). Living Workspace korundu (PASS). Single Authority korundu (PASS — `payment.apply`/`collection.set_lifecycle` aynı, tek yollar; ikinci authority yok). Kullanıcı gerçekten yeni bir yaşayan davranış kazandı mı? PASS — geciken ve kısmi tahsilatlar artık gerçekten production'da yaşıyor, uydurma veri olmadan kanıtlandı. **FAIL yok → ACCEPTED.**

**Deliberately not built** (documented, not a defect): `collection.start`, a dedicated OVERDUE-badge/KPI card on the collections list (constitution's "Gecikmiş Alacaklar" KPI — data now exists via `status`, UI aggregation not built), negative-path re-verification of the new due-date regex edge cases beyond what's covered by production testing above.

## 12. Next living behavior — priority note (user directive, 2026-08-02)

Before starting any new domain, the user wants the **Executive Presence / Executive Conversation** behavior fixed in production: during this session's acceptance testing, METRIX lost its own context mid-conversation and answered a follow-up question about its own prior message ("bugünün öncelikleri" screen) like a generic, context-free LLM — asking the user to clarify which "screen" they meant, instead of recognizing its own just-stated reference. The user identified this as an Executive Trust violation (§1 doctrine: METRIX must never behave like a context-free general-purpose assistant). This is now the **next planned operation** (not started, not scoped yet) — ranked above any further new-domain work (Fatura SENT, Muhasebe, Raporlama, etc. from §4, which remain valid but lower priority until this is fixed).
